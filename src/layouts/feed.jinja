{% extends "base.jinja" %}
{% block content %}
  <h1>Blog</h1>
  <ul>
  {% for post in collections.post|sort(attribute="date") %}
    <li><a href="{{ post.url }}">{{ post.title }} - {{ post.date }} </a></li>
  {% endfor %}
  </ul>
{% endblock %}